# JobTread Pave API grant key (create in JT: settings -> API / integrations).
JT_GRANT_KEY=

# Shared secret the mobile app sends as the `x-app-token` header.
# Generate one: openssl rand -hex 24
APP_TOKEN=

# Optional overrides
PORT=8787
